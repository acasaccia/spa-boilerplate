machine:
  environment:
    API_BASE_URL: http://localhost:1234
test:
  override:
    - npm start:
        background: true
    - npm run mockserver:
        background: true
    - sleep 60
    - npm test
